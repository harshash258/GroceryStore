<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mycompany.grocerystore">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.HousingNeeds"/>
        <activity android:name=".Activities.DairyProduct" />
        <activity android:name=".Activities.BeautyandBeast" />
        <activity android:name=".Activities.IceCream" />
        <activity android:name=".Activities.PackagedFood" />
        <activity android:name=".Activities.CookingEssentials" />
        <activity android:name=".Activities.DisplayOrderDetails" />
        <activity android:name=".Activities.DisplayProductDetails"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".Activities.CartActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".Activities.NavigationMenu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.UploadUserDetails"
            android:noHistory="true" />
        <activity android:name=".Activities.VerifyPhoneNumber" />
        <activity android:name=".Activities.RegisterPhoneNumber" />
        <activity
            android:name=".Activities.SplashScreenActivity"
            android:noHistory="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>